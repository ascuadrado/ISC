Raspberry Pi Basic Configuration:

Hostname: iscms.local
Username: pi
Password: motero

Wifi SSID: Alberto SC
Wifi Pswd: albertos

+ Activate ssh
+ Activate vnc
+ Activate spi

Conexiones:
CAN0:
	Int0->GPIO24
	CS0 ->GPIO7
CAN1:

	Int0->GPIO25
	CS0 ->GPIO8

Setup:
	Funciona automáticamente:
	- Ejecuta main.py y lo hace todo
	(necesita tener 2 carpetas en el desktop: Rasp-main y sketch_0_test)
	Tarda 55 seg en arrancar

----------------------------------

Some useful tools for managing project in Raspberry Pi:

Clone github:

git clone https://github.com/ascuadrado/ISC

Prepare proper file structure: 

cp -R ISC/Rasp-main .
cp -R ISC/Processing/sketch_0_test .

Send files over ssh:

scp -r /Users/ascuadrado/Dropbox/ICAI/ISC/ISC/Processing/sketch_0_test pi@iscms.local:Desktop
scp -r /Users/ascuadrado/Dropbox/ICAI/ISC/ISC/Rasp-main pi@iscms.local:Desktop

Receive files over ssh

scp -r pi@iscms.local:Desktop /Users/ascuadrado/Desktop

Remove rpi files:
rm -r Desktop/sketch_0_test
rm -r Desktop/Rasp-main

Execute python main

python3 Desktop/Rasp-main/main.py

Execute processing

export DISPLAY=:0.0
Desktop/sketch_0_test/application.linux32/sketch_0_test

Automatic run on reboot

crontab -e -> Add to file:
@reboot python3 /home/pi/Desktop/Rasp-main/main.py > /home/pi/Desktop/Rasp-main/log.txt



----------------------------------

Set up can tools in Raspberry Pi:

1. Install can-utils:
sudo apt-get install can-utils

2. Install python-can
pip3 install python-can

3. Install sqlite3
sudo apt-get install sqlite3

4. Install java (for processing - used for display)
sudo apt install openjdk-8-jdk

5. Avoid screen sleep
Append to file: /etc/xdg/lxsession/LXDE-pi/autostart

@xset s off
@xset -dpms

6. Setup of HDMI Screen (only when we want the display to work)
Append to file: /boot/config.txt

# HDMI Screen
hdmi_group=2
hdmi_mode=87
hdmi_cvt=800 480 60 6 0 0 0
hdmi_drive=1

7. Set CAN hardware:
Append to file: /boot/config.txt

dtparam=spi=on
dtoverlay=mcp2515-can0,oscillator=8000000,interrupt=25
dtoverlay=mcp2515-can1,oscillator=8000000,interrupt=24

8. Start CAN Communication (every time the raspberry boots):
(this step is executed in main.py)

sudo ip link set can0 up type can bitrate 250000
sudo ip link set can1 up type can bitrate 500000
